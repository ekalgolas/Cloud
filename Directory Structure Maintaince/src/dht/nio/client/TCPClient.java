package dht.nio.client;

public class TCPClient {
	// public TCPConnection openConnection(String ip, int port) throws
	// IOException {
	// TCPConnection connection = new TCPConnection();
	// connection.bind(ip, port);
	// return connection;
	// }
}
